# Lokale (Host-)Artefakte NICHT ins Build-Image — sonst werden im build-stage
# die frisch per `npm ci` installierten Linux-node_modules überschrieben.
node_modules
.git
dist
.env
*.log
data
*.db
*.db-wal
*.db-shm
.github
.claude
docker-compose.yml
README.md
